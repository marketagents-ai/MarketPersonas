{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "GSS Survey Section: NEWCELPH",
  "description": "Questions related to NEWCELPH",
  "properties": {
    "NEWCELPH": {
      "type": "string",
      "title": "NEWCELPH",
      "description": "$(document).ready(function() {$('input[type=\"text\"].mrEdit').before('Phone number: ');});What is that phone number? End If End If If PHONLOC = {YES} Then If (PHNETYPE = {CELLULAR} And REGPHONE = {YES}) Or (PHNETYPE = {REGULAR} And CELPHONE = {YES}) Then"
    }
  },
  "required": [
    "NEWCELPH"
  ]
}